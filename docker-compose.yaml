version: '2'
services:
  app1:
    image: dockerfile
    command: /app/plumber.R
    volumes:
     - .:/app
    restart: always
    ports:
     - "8000:8000"       
  app2:
    image: dockerfile
    command: /app/plumber.R
    volumes:
     - .:/app
    restart: always
    ports:
     - "8001:8000"       
  app3:
    image: dockerfile
    command: /app/plumber.R
    volumes:
     - .:/app
    restart: always  
    ports:
     - "8002:8000"    
  rstudio:
    image: rocker/rstudio
    volumes:
     - .:/app
    restart: always
    ports:
     - "8787:8787"